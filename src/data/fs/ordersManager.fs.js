// id del objeto
// id del producto (pid)
// id del usuario (uid)
// quantity (cantidad que ordena el usuario)
// state (estado de la compra) "Entregado, comprado"

// create(data)
// read()
// readOneOrder(uid)
// update(oid, quantity, state)
// destroy(oid)
